{
    "extends": "./tslint.json",
    "defaultSeverity": "warning",
    "rules": {
        "no-console": [false],
        "max-line-length": [
            true,
            {
                "limit": 200,
                "ignore-pattern": "^import |^export {(.*?)}"
            }
        ]
    }
}
