{
    "extends": "./tslint.json",
    "defaultSeverity": "warning",
    "rules": {
        "max-line-length": [
            true,
            {
                "limit": 200,
                "ignore-pattern": "^import |^export {(.*?)}"
            }
        ]
    }
}
