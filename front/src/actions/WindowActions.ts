import { action } from 'mobx';

export class WindowActionsClass {
    @action
    static setBoardWidth = async (): Promise<boolean> => {
        // TODO

        return true;
    };

    constructor() {
        //
    }
}
